
import { createImage } from 'ember-weather/helpers/create-image';
import { module, test } from 'qunit';

module('Unit | Helper | create image');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = createImage([42]);
  assert.ok(result);
});

