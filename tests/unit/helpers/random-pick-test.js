
import { randomPick } from 'ember-weather/helpers/random-pick';
import { module, test } from 'qunit';

module('Unit | Helper | random pick');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = randomPick([42]);
  assert.ok(result);
});

