
import { bringUmbrella } from 'ember-weather/helpers/bring-umbrella';
import { module, test } from 'qunit';

module('Unit | Helper | bring umbrella');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = bringUmbrella([42]);
  assert.ok(result);
});

