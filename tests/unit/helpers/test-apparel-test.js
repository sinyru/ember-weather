
import { testApparel } from 'ember-weather/helpers/test-apparel';
import { module, test } from 'qunit';

module('Unit | Helper | test apparel');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = testApparel([42]);
  assert.ok(result);
});

