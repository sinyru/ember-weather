
import { testLocationExist } from 'ember-weather/helpers/test-location-exist';
import { module, test } from 'qunit';

module('Unit | Helper | test location exist');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = testLocationExist([42]);
  assert.ok(result);
});

