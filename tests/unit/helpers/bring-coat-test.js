
import { bringCoat } from 'ember-weather/helpers/bring-coat';
import { module, test } from 'qunit';

module('Unit | Helper | bring coat');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = bringCoat([42]);
  assert.ok(result);
});

