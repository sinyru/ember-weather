
import { tempConvert } from 'ember-weather/helpers/temp-convert';
import { module, test } from 'qunit';

module('Unit | Helper | temp convert');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = tempConvert([42]);
  assert.ok(result);
});

